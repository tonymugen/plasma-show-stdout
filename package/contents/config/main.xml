<?xml version="1.0" encoding="UTF-8"?>
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
      http://www.kde.org/standards/kcfg/1.0/kcfg.xsd">
    <kcfgfile name=""/>
    <group name="General">
        <!--
            The full, ordered module list, serialized as a JSON array of objects:
                [{ "kind": 0|1, "script": "/path", "interval": <s>, "signalOffset": <N>, "outputLimit": <chars> }, ...]
            kind 0 = timed polling (uses "interval" seconds), 1 = realtime signal
            (uses "signalOffset", listened on as SIGRTMIN+N). "outputLimit" is the
            per-script character cap on displayed output. A variable-length list
            does not fit a fixed set of scalar cfg_<key> entries, so it lives in one
            String key; configGeneral.qml edits an in-memory ListModel and re-encodes
            it here, and main.qml decodes it to build the setModules() argument.
        -->
        <entry name="modules" type="String">
            <label>Configured script modules, encoded as a JSON array.</label>
            <default>[]</default>
        </entry>
        <!--
            Single separator inserted between adjacent module outputs in the
            combined display text. Surrounding spaces are significant; the default
            " | " matches the historical hard-wired separator.
        -->
        <entry name="delimiter" type="String">
            <label>Separator inserted between adjacent script outputs.</label>
            <default> | </default>
        </entry>
        <!--
            Font family used to render the output (panel and popup). The default
            "monospace" is the generic fixed-width alias; set a specific family
            (e.g. a Nerd Font) so glyphs outside the default font are displayed.
        -->
        <entry name="fontFamily" type="String">
            <label>Font family used to display script output.</label>
            <default>monospace</default>
        </entry>
        <!--
            Point size of the displayed output. 0 means "use the theme's default
            font size" so an unconfigured widget looks exactly as before.
        -->
        <entry name="fontSize" type="Int">
            <label>Point size used to display script output (0 = theme default).</label>
            <default>0</default>
        </entry>
        <!-- Render the output in bold / italic (font "style"). -->
        <entry name="fontBold" type="Bool">
            <label>Display script output in bold.</label>
            <default>false</default>
        </entry>
        <entry name="fontItalic" type="Bool">
            <label>Display script output in italic.</label>
            <default>false</default>
        </entry>
    </group>
</kcfg>
